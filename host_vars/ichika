proc_version: armv7
has_fan: no

disks:
  - /dev/sda

ceph_disks:
  - /dev/sda1

keepalived_vips:

  - id: "{{ research_vip_id }}"
    name: research_vip
    ip: "{{ research_vip_ip }}"
    priority: 200
    state: MASTER
    iface: eth0
    password: "{{ research_vip_password }}"
