proc_version: armv7
has_fan: yes
external_connections: yes

keepalived_vips:

  - id: "{{ public_vip_id }}"
    name: public_vip
    ip: "{{ public_vip_ip }}"
    priority: 190
    state: BACKUP
    iface: eth0
    password: "{{ public_vip_password }}"
