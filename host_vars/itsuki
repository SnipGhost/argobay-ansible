proc_version: armv7
has_fan: yes

disks:
  - /dev/sda

keepalived_vips:

  - id: "{{ dev_private_vip_id }}"
    name: dev_private_vip
    ip: "{{ dev_private_vip_ip }}"
    priority: 200
    state: MASTER
    iface: eth0
    password: "{{ dev_private_vip_password }}"
