proc_version: armv7
has_fan: true

keepalived_vips:
  - id: "{{ public_vip_id }}"
    name: public_vip
    ip: "{{ public_vip_ip }}"
    mask: "{{ public_vip_mask }}"
    priority: 180
    state: BACKUP
    iface: eth0
    password: "{{ public_vip_password }}"
