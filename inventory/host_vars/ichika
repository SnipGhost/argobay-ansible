proc_version: armv7
has_fan: no

disks:
  - /dev/sda

keepalived_vips:

  - id: "{{ research_vip_id }}"
    name: research_vip
    ip: "{{ research_vip_ip }}"
    priority: 200
    state: MASTER
    iface: wlan0
    password: "{{ research_vip_password }}"
