proc_version: armv7
has_fan: no
external_connections: yes

keepalived_vips:

  - id: "{{ public_vip_id }}"
    name: public_vip
    ip: "{{ public_vip_ip }}"
    priority: 200
    state: MASTER
    iface: eth0
    password: "{{ public_vip_password }}"
