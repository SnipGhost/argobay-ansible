proc_version: armv7
has_fan: yes

keepalived_vips:

  - id: "{{ dev_public_vip_id }}"
    name: dev_public_vip
    ip: "{{ dev_public_vip_ip }}"
    priority: 200
    state: MASTER
    iface: eth0
    password: "{{ dev_public_vip_password }}"
