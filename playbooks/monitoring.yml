---
- hosts: monitoring_servers
  roles:
    # - name: prometheus
    #   tags: prometheus
    - name: victoria_metrics
      tags:
        - victoria_metrics
        - victoria-metrics
        - prometheus
    - name: alertmanager
      tags: alertmanager
    - name: alertmanager_bot
      tags: alertmanager_bot
    - name: grafana
      tags: grafana
