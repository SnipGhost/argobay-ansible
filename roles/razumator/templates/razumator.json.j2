{
	"PORT": "{{ razumator_port }}",
	"HOST": "{{ razumator_host }}",
	"ALLOWED_HOSTS": ["127.0.0.1:{{ razumator_api_port }}", "localhost:{{ razumator_api_port }}"],
	"PATH_TO_MODELS": "{{ razumator_model_path }}",
	"MODEL_SERVER_HOST": "localhost",
	"MODEL_SERVER_PORT": "8001"
}