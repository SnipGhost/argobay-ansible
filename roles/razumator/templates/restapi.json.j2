{
	"PORT": "{{ razumator_api_port }}",
	"HOST": "{{ razumator_api_host }}",

	"PATH_TO_STATIC": "{{ razumator_static_path }}",
	"PATH_TO_MODELS": "{{ razumator_model_path }}",

	"MODEL_SERVER_HOST": "localhost",
	"MODEL_SERVER_PORT": "8001",

	"RAZUM_SERVER_HOST": "{{ razumator_host }}",
	"RAZUM_SERVER_PORT": "{{ razumator_port }}",

	"DB_NAME": "{{ razumator_db_name }}",
	"DB_USERNAME": "{{ razumator_db_user }}",
	"DB_PASSWORD": "{{ razumator_db_pass }}",
	"DB_URL": "{{ mariadb_proxy_host }}:{{ mariadb_proxy_port }}",

	"MEMCACHED_URL": "{{ memcached_proxy_host }}:{{ memcached_proxy_port }}"
}