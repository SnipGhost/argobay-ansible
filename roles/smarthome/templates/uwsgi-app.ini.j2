[uwsgi]
name = {{ app_name }}
uid = assol
gid = assol

master = true
workers = 2
threads = 2

http-socket = {{ smarthome_bind }}:{{ smarthome_port }}
buffer-size = {{ smarthome_buffer_size }}

need-app = true
lazy-apps = true

chdir = {{ smarthome_bindir }}
logto2 = {{ smarthome_logdir }}/smarthome.log

home = {{ smarthome_bindir }}/env
plugin = python3
callable = app
wsgi-file = {{ smarthome_bindir }}/run.py
